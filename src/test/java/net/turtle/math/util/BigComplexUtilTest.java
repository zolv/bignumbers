package net.turtle.math.util;

import org.junit.Test;

public class BigComplexUtilTest {
	
	
	@Test
	public void testToStringNormalized() {
		// Assert.assertEquals("2", BigRationalUtil.toStringNormalized(new
		// BigComplex(new BigRational)));
		// Assert.assertEquals("-2", BigRationalUtil.toStringNormalized(new
		// BigRational("2/-1")));
		// Assert.assertEquals("2/3", BigRationalUtil.toStringNormalized(new
		// BigRational("2", "3")));
		// Assert.assertEquals("-2/3", BigRationalUtil.toStringNormalized(new
		// BigRational("-2", "3")));
		// Assert.assertEquals("-2/3", BigRationalUtil.toStringNormalized(new
		// BigRational("2", "-3")));
		// Assert.assertEquals("2/3", BigRationalUtil.toStringNormalized(new
		// BigRational("-2", "-3")));
	}
	
}
